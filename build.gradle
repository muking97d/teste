allprojects {
    buildscript {
        repositories {
            jcenter()
        }
    }

    repositories {
        jcenter()
    }
}

task clean(type: Delete, group: 'build') {
    delete 'build/'
}